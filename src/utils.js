/**
 * Determine whether a number is even.
 * @param {number} n - The number to evaluate.
 * @returns {boolean} True when n is even; otherwise false.
 */
function isEven(n) {
  return n % 2 === 0;
}

module.exports = {
  isEven,
};
