You are the orchestrator for AgentSDLC. A step has failed and you need to plan minimal rework steps.

## Available Agents
- Product Agent (id: "product", role: "product"): Analyze feature requests and bug reports, Write product requirement documents, Define user stories and acceptance criteria
- Architecture Agent (id: "architect", role: "architect"): Design system architecture, Create data models and ER diagrams, Define API contracts (OpenAPI/YAML)
- Developer Agent (id: "developer", role: "developer"): Write production TypeScript/JavaScript code, Build React/Next.js frontend components, Build API routes and server-side logic
- QA Agent (id: "qa", role: "qa"): Write unit tests (vitest), Write integration/API tests, Write E2E tests (playwright)
- Security Agent (id: "security", role: "security"): Static analysis security scanning, Dependency vulnerability checking, Auth flow review
- UI/UX Agent (id: "ui-ux", role: "ui-ux"): Create wireframes and user flows, Design component specifications, Generate React component previews
- DevOps Agent (id: "devops", role: "devops"): Create/modify CI/CD pipelines, Write Dockerfiles and compose files, Configure cloud infrastructure (Terraform/Pulumi)

## Completed Steps
- Step 1 (ui-ux): Designed the SprintFoundry marketing website UI for website-v1. Produced a complete dark-theme design system with 6 agent accent colors, user flows for 3 visitor personas, and detailed component specifications for all 13 components across the 4 required sections (Hero, Features, How It Works, Pricing) plus global navigation and footer. All states, responsive behavior, animations, and WCAG 2.1 AA accessibility requirements are documented.
- Step 2 (developer): Built the SprintFoundry marketing website in website-v1/ using Next.js 16 (App Router), TypeScript, and Tailwind CSS v4. Implemented all required sections: Hero with animated AgentOrbitDiagram (SVG hexagonal agent visualization cycling through agents), Features section with 6 agent cards (purple/blue/green/yellow/red/pink accent colors), How It Works 4-step pipeline with connecting flow, Pricing section with 3 tiers and Monthly/Annual toggle, plus SiteNav (scroll-spy + mobile drawer), StatsBanner (count-up animations), and Footer. Build passes TypeScript and Next.js checks cleanly.
- Step 901 (product): Pricing conflict resolved. The '$49/mo, 10 tickets' figure cited by QA does not appear in the original ticket or any prior agent artifact — it is untraced. The UI/UX agent's $79/mo Pro specification (from artifacts/ui-specs/components.md) is the only documented pricing in the pipeline and is adopted as canonical. No changes to the current PricingSection.tsx implementation are required for pricing. Produced authoritative pricing decision document, user stories (3 stories, 10 acceptance criteria), and scope breakdown.
- Step 902 (developer): Fixed three issues in website-v1: (1) Pricing section was already correct per canonical spec — no code change needed; (2) Added smooth-scroll onClick to the Watch Demo button targeting #how-it-works; (3) Implemented a native focus trap in SiteNav mobile drawer — focus moves in on open, Tab/Shift+Tab is constrained within the drawer, and focus returns to hamburger on close. Build passes.

## Failed Step
Step 3 (qa): Validate the SprintFoundry marketing website in website-v1. Check: (1) All required sections are present (hero, features, how-it-works, pricing), (2) All 6 agent types are represented in the features section with distinct styling, (3) Pricing section has all three tiers (Free, Pro, Enterprise), (4) The site builds without errors (run npm run build), (5) Responsive layout renders correctly at mobile (375px), tablet (768px), and desktop (1440px) breakpoints, (6) No console errors or missing imports, (7) Dark theme is consistently applied, (8) Navigation links are functional. Report any bugs found.
Status: needs_rework
Reason: MAJOR bug found: All three pricing CTA buttons (Start Building Free, Start Free Trial, Contact Sales) navigate to '#' instead of the correct destinations specified in product spec US-3. This means clicking any pricing CTA causes the page to scroll to the top rather than routing users to signup or contact flows.
Issues: MAJOR: Pricing CTA hrefs all use '#' instead of spec-required /signup, /signup?plan=pro, /contact (product spec US-3, PricingSection.tsx:27/45/65); MINOR: Emoji characters in AgentOrbitDiagram SVG may render incorrectly on Windows Chrome (pre-existing, acknowledged by dev)

## Rework Attempt 2 of 3

## Previous Rework Attempts
### Attempt 1
Status: needs_rework
Summary: 83 tests written and all passing. Build clean (Next.js 16 TypeScript). All 4 required sections present, 6 agents with distinct styling, 3 pricing tiers, dark theme consistent. 1 major issue (pricing spec conflict), 3 minor issues (non-functional Watch Demo button, missing focus trap, cross-platform emoji rendering). No critical bugs.
Issues: MAJOR: Pricing spec mismatch — task spec says $49/mo (10 tickets) but implementation shows $79/mo (unlimited tickets). Developer followed a separate UI spec artifact. Product team must confirm canonical pricing before launch.; MINOR: 'Watch Demo' button in HeroSection has no href or onClick handler — clicking it does nothing; MINOR: Mobile navigation drawer has no focus trap — Tab key cycles through elements behind the drawer, violating the accessibility spec in dev-to-qa.md; MINOR: AgentOrbitDiagram uses emoji characters in SVG <text> elements which may render as blank boxes on Windows — known limitation noted by developer

### Attempt 2
Status: needs_rework
Summary: 105 tests run (102 passed, 3 failed). Build passes cleanly. Both dev-sprint fixes verified: Watch Demo scroll and mobile focus trap work correctly. One new MAJOR bug found: all three pricing CTA buttons use href='#' instead of the spec-required /signup, /signup?plan=pro, and /contact destinations (product spec US-3). Emoji rendering in AgentOrbitDiagram is a pre-existing MINOR issue.
Issues: MAJOR: Pricing CTA hrefs all use '#' instead of spec-required /signup, /signup?plan=pro, /contact (product spec US-3, PricingSection.tsx:27/45/65); MINOR: Emoji characters in AgentOrbitDiagram SVG may render incorrectly on Windows Chrome (pre-existing, acknowledged by dev)

Return ONLY a valid JSON array of 1-2 rework steps. No markdown fences.
Each step: { "step_number": 900+N, "agent": "id", "task": "description", "context_inputs": [{"type":"ticket"},{"type":"step_output","step_number":N}], "depends_on": [], "estimated_complexity": "medium" }